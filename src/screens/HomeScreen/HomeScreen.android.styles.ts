import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
  },
  flatList: {
    flex: 1,
    width: '100%',
  },
  safeArea: {
    minHeight: '100%',
  },
});
