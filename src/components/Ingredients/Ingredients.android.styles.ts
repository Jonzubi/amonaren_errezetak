import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inputsView: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginVertical: 25,
  },
});
